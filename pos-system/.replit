run = "npm start"
entrypoint = "server/index.js"

[env]
PORT = "3000"
NODE_ENV = "development"
JWT_SECRET = "pos-jwt-secret-replit-2025"
JWT_EXPIRES_IN = "8h"
DB_PATH = "./data/pos.db"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "npm start"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 3000
externalPort = 80
